import { StudentMarksPipe } from './student-marks.pipe';

describe('StudentMarksPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentMarksPipe();
    expect(pipe).toBeTruthy();
  });
});
