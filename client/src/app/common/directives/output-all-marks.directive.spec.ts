import { OutputAllMarksDirective } from './output-all-marks.directive';

describe('OutputAllMarksDirective', () => {
  it('should create an instance', () => {
    const directive = new OutputAllMarksDirective();
    expect(directive).toBeTruthy();
  });
});
