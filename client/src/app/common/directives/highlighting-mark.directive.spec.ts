import { HighlightingMarkDirective } from './highlighting-mark.directive';

describe('HighlightingMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightingMarkDirective();
    expect(directive).toBeTruthy();
  });
});
